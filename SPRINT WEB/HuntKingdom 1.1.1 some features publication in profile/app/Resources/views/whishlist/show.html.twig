{% extends 'base.html.twig' %}

{% block body %}
    <h1>Whishlist</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ whishlist.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('whishlist_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('whishlist_edit', { 'id': whishlist.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
