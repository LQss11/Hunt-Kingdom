{% extends 'Back/Back.html.twig' %}

{% block content %}
    <h1>Espece creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('espece_show') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
